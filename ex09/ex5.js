/*
    Math -> Matematica
        - pow(2,2) / potencia / 2(elvado a 2) = 4
        - sqrt(25,2) / raiz / (raiz de 25)
        - ceil / teto / arredondar para cima
        - floor / chao / arredondar para baixo
        - random() / numero aleatorio entre 0 e 1
*/

/*
const calculo = Math.pow(2,2)

console.log(calculo);
*/

/*
const calculo = Math.sqrt(125,2)

console.log(Math.ceil(calculo));
*/

/*
const calculo = Math.sqrt(125,2)

console.log(Math.floor(calculo));
*/

/* Calculo de pi
const pi = Math.PI

console.log(pi)
*/
//              Math.random() * (max - min)+ min;
const calculo = Math.random() * (100 - 1 ) + 1;
console.log(Math.ceil(calculo));

